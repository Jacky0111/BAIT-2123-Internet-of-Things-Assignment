<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-feature
        android:name="android.hardware.sensor.proximity"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".TemperatureHumidity"></activity>
        <activity android:name=".SoilHumidity" />
        <activity android:name=".SmartDoor" />
        <activity android:name=".CCTV" />
        <activity android:name=".LightProximity" />
        <activity android:name=".SmartAgriculture" />
        <activity android:name=".ParkingSensor" />
        <activity android:name=".InGround" />
        <activity android:name=".SmartBuilding" />
        <activity android:name=".SmartStreet" />
        <activity android:name=".SmartParking" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>